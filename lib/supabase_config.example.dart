// Template for lib/supabase_config.dart -- copy this file to
// lib/supabase_config.dart and fill in your real project values there.
// lib/supabase_config.dart is gitignored, so your keys never get committed.
//
//   cp lib/supabase_config.example.dart lib/supabase_config.dart
//
// Then fill in the two values below in your copy. See lib/supabase_config.dart's
// own header comment (or the README) for where to find these in your
// Supabase project dashboard.

class SupabaseConfig {
  static const supabaseUrl = 'REPLACE_ME'; // e.g. https://xxxxxxxx.supabase.co
  static const supabaseAnonKey = 'REPLACE_ME';
}
